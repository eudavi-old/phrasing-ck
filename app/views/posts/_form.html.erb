<%= form_with(model: post, local: true) do |form| %>
	<div class="form-group">
		<div class="col-sm-8">
			<%= cktext_area_tag 'descricao', @post.descricao, ckeditor: {toolbar: 'Full'} %>
		</div>
	</div>

	<%= form.submit 'Enviar' %>
<% end %>
